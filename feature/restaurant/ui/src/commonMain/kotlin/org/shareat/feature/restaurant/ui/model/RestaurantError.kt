package org.shareat.feature.restaurant.ui.model

enum class RestaurantError {
    INVALID_CREDENTIALS,
    OFFLINE,
    UNAUTHENTICATED,
    FORBIDDEN,
    TEMPORARILY_UNAVAILABLE,
    ALREADY_EXISTS,
    NOT_FOUND,
    UNKNOWN,
}
