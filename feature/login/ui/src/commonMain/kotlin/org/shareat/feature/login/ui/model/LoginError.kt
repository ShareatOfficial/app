package org.shareat.feature.login.ui.model

enum class LoginError {
    INVALID_INPUT,
    EMAIL_REQUIRED,
    INVALID_CREDENTIALS,
    OFFLINE,
    UNAUTHENTICATED,
    FORBIDDEN,
    TEMPORARILY_UNAVAILABLE,
    ALREADY_EXISTS,
    NOT_FOUND,
    UNKNOWN,
}
