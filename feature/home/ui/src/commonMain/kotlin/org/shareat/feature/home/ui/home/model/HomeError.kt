package org.shareat.feature.home.ui.home.model

enum class HomeError {
    INVALID_CREDENTIALS,
    OFFLINE,
    UNAUTHENTICATED,
    FORBIDDEN,
    TEMPORARILY_UNAVAILABLE,
    ALREADY_EXISTS,
    NOT_FOUND,
    UNKNOWN,
}
